#include <stdio.h>
/**
 * main - Program that prints single digit numbers of base 10 stsarting from 0
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int c;

	c = 0;
	while
		(c < 10) {
			putchar(c + '0');
			c++;
		}
	putchar('\n');
	return (0);
}
